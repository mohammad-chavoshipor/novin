export const BASE_URL = 'https://reqres.in';
export interface ProfileSTRC {
    id: string,
    email: string,
    first_name: string,
    last_name: string,
    avatar: string
}
export type Param = {
    id: string;
};